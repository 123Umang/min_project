import React from "react";
import Card from "../components/Card";

export default function Admin() {
  return (
    <div>
      <h2>Employee / Admin View</h2>
      <Card>
        <div className="muted">Admin dashboard preview (transactions, user management)</div>
      </Card>
    </div>
  );
}
